@model IEnumerable<BibliotecaAppV2.Models.Editora>
@{
    ViewData["Title"] = "Editoras";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Nova Editora</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Descrição</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Any())
        {
            foreach (var editora in Model)
            {
                <tr>
                    <td>@editora.Nome</td>
                    <td>@editora.Descricao</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@editora.Id" class="btn btn-sm btn-warning">Editar</a>
                        <a asp-action="Details" asp-route-id="@editora.Id" class="btn btn-sm btn-info">Detalhes</a>
                        <a asp-action="Delete" asp-route-id="@editora.Id" class="btn btn-sm btn-danger">Excluir</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3" class="text-center text-muted">Nenhuma editora cadastrada.</td>
            </tr>
        }
    </tbody>
</table>